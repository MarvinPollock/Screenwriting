<%= form_for(@frame) do |f| %>
  <% if @frame.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@frame.errors.count, "error") %> prohibited this frame from being saved:</h2>

      <ul>
      <% @frame.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

    <div class="field">
      <%= f.label :image %><br />
      <%= f.file_field :image %>
    </div>

  <div class="field">
    <%= f.label :descr %><br />
    <%= f.text_field :descr %>
  </div>
    <div class="field">
      <%= f.label :p_name %><br />
      <%= f.text_field :p_name %>
    </div>
    <div class="field">
      <%= f.label :pad_url %><br />
      <%= f.text_field :pad_url, :value => @random_text, :readonly => true  %>
    </div>

    <div class="field">
      <%= f.label :pad_id %><br />
      <%= f.text_field :pad_id, :value => @pad.id, :readonly => true  %>
    </div>

    <div class="actions">
      <%= f.submit %>
    </div>

<% end %>
